# Roadmap проекта Expenses

План разработки по **срезам**. Срез = тонкая сквозная функциональность, которая уже работает. Наращиваем слоями, не строим всё сразу.

Формат работы — см. [../CLAUDE.md](../CLAUDE.md): гибрид, обвязку/инфру делает Claude и объясняет, логику пишет пользователь.

Легенда: ✅ сделано · 🟨 в процессе · ⬜ не начато.

---

## Срез 0 — «шагающий скелет»
Цель: убедиться, что весь путь `домен → nginx → сертификат → Ktor` живой, ещё без бизнес-логики.

### 0а. Локальный каркас — ✅ СДЕЛАНО (2026-07-15, делал Claude)
- ✅ `backend/` — самостоятельный Gradle-проект на Ktor 3.0.3 (Kotlin 2.1.0), wrapper Gradle 8.14.3.
- ✅ Эндпоинт `GET /health` отвечает `{"status":"ok"}`.
- ✅ Сервер слушает `127.0.0.1:8080` (наружу отдаёт nginx).
- ✅ Собрано (`BUILD SUCCESSFUL`) и **проверено запуском** фэт-джара: `HTTP 200 {"status":"ok"}`.
- ✅ Репозиторий создан и запушен: https://github.com/vihrea1337/expenses (private). Коммиты: каркас + `.gitattributes` (единые переносы строк, чтобы `gradlew` работал на Linux).

Как проверить локально:
```powershell
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
cd C:\Users\main\Desktop\GitArea\Expenses\backend
.\gradlew.bat run
# затем открыть http://127.0.0.1:8080/health ; остановить — Ctrl+C
```

### 0б. Деплой на VPS — ⬜ СЛЕДУЮЩИЙ ШАГ
Делаем на сервере (Claude даёт команды — пользователь выполняет по SSH). Подзадачи:
- ⬜ Проверить/установить на VPS Java 17+.
- ⬜ Собрать фэт-джар (`.\gradlew.bat buildFatJar` → `build/libs/expenses-backend-all.jar`) и залить на сервер.
- ⬜ Настроить поддомен в существующем nginx (блок `server` с `proxy_pass` на `127.0.0.1:8080`) — конфиг в [architecture.md §6](architecture.md).
- ⬜ Выпустить сертификат через certbot для поддомена.
- ⬜ Создать systemd-юнит, чтобы jar работал круглосуточно.
- ⬜ Проверить: `https://<поддомен>/health` отвечает `{"status":"ok"}`.

**Домашка пользователя перед 0б:**
1. Создать бота: Telegram → **@BotFather** → `/newbot` → получить **токен** (сохранить, никому не показывать). *(Токен понадобится в Срезе 1, но лучше сделать сразу.)*
2. Завести **поддомен** для VPS (DuckDNS или поддомен своего домена), направить на IP сервера.
3. Узнать, установлена ли на VPS **Java** (если нет — поставим).

---

## Срез 1 — одна трата end-to-end — ⬜
Цель: трата, введённая в боте, видна в приложении, и наоборот. Здесь **логику пишет пользователь** (Claude объясняет и ревьюит).

- ⬜ Таблица `expenses` в PostgreSQL (через Exposed): id (UUID), сумма, категория, заметка, дата.
- ⬜ `POST /api/expenses` — добавить трату (из приложения).
- ⬜ `GET /api/expenses` — список трат (для приложения).
- ⬜ Webhook `POST /telegram/<секрет>` — принять «кофе 200», разобрать на категорию+сумму, записать в БД, ответить в чат.
- ⬜ Android-экран: список трат + поле для добавления (Retrofit-клиент к API). Room-кэш пока не делаем.

Роли: Claude заранее поднимет PostgreSQL и подключит Exposed (обвязка); эндпоинты, парсер и Android-экран пишет пользователь.

---

## Дальше — улучшения слоями — ⬜
Привязка аккаунтов + авторизация · UUID+идемпотентность · офлайн-first + синхронизация · умный парсер (один на сервере) · реалтайм (push/polling) · общий Kotlin-модуль моделей · бюджеты/экспорт/графики. Подробности и приоритеты — [architecture.md §8](architecture.md).

---

## ▶ Продолжить отсюда (для новой сессии)
1. Открыть этот проект; прочитать `CLAUDE.md`, `docs/architecture.md`, этот файл.
2. Текущая точка: **Срез 0б — деплой на VPS.**
3. Спросить пользователя, готова ли домашка (токен бота, поддомен, есть ли Java на VPS).
4. Дальше по шагам 0б: Claude выдаёт команды для nginx/certbot/systemd — пользователь выполняет на сервере.
5. После зелёного `https://<поддомен>/health` — переходим к Срезу 1 (логику пишет пользователь).

## Открытые вопросы
- **Соавторство в коммитах.** Сейчас в коммитах стоит трейлер `Co-Authored-By: Claude`. В проекте Suomeksi пользователь просил коммитить без него. Если для портфолио нужно «чистое» авторство — убрать (репозиторий свежий, переписать 2 коммита безопасно). Решение пользователя ожидается.
