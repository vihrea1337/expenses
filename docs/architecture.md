# Архитектура проекта Expenses

Документ описывает **что** мы строим, **как** оно устроено и **почему** приняты именно такие решения. Термины расшифрованы; в конце — глоссарий.

---

## 1. Идея продукта
Учёт личных расходов **быстрым вводом**. Главная ценность — минимум трения: не надо открывать приложение, чтобы записать трату, — достаточно строчки в Telegram («кофе 200»). При этом есть и полноценное Android-приложение. Оба входа пишут в **общие данные**.

## 2. Главный архитектурный принцип
> Как только данные заносятся из ДВУХ мест (телефон + бот) и должны быть видны в обоих — им нужен **один общий источник правды, и живёт он на сервере, а не на телефоне.**

Это отличает Expenses от проекта SkyCast, где источником правды был сам телефон (offline-кэш). Здесь так нельзя: телефон не знает, что ты ввёл через бота, пока данные не сведены в одном месте на сервере.

Важное уточнение: **бот — это не база данных.** Бот — просто ещё один клиент Telegram, как и приложение. Поэтому схема не «приложение ← бот», а «приложение ↔ **общий бэкенд** ↔ бот». Причём бот и API для приложения — это **одна и та же программа на Kotlin**.

## 3. Схема
```
  ┌───────────────┐                 ┌──────────────────────────────┐         ┌────────────┐
  │  Android-app  │ ── REST API ──► │      Бэкенд на Kotlin (Ktor) │ ── SQL ─►│ PostgreSQL │
  │ Compose       │ ◄────────────── │  = Telegram-бот              │ ◄────── │  (данные)  │
  │ (+Room-кэш)   │                 │  + HTTP API для приложения   │         └────────────┘
  └───────────────┘                 └───────────────┬──────────────┘
                                                     │ Telegram Bot API
                                                     ▼
                                              ┌────────────┐
                                              │  Telegram  │ ◄─ пользователь пишет «кофе 200»
                                              └────────────┘
```
И бот, и приложение — просто **клиенты**. Данными владеет бэкенд.

## 4. Компоненты
- **backend/ (Ktor)** — сервер на Kotlin. Один процесс делает две вещи:
  1. принимает обновления от Telegram (логика бота);
  2. отдаёт REST API для Android-приложения.
  Оба пишут/читают общую БД.
- **PostgreSQL** — база данных (единый источник правды). Доступ из Kotlin — через **Exposed** (библиотека, позволяет писать запросы на Kotlin, а не голым SQL).
- **androidApp/ (Compose)** — Android-приложение; ходит в API бэкенда (Retrofit). Позже добавим локальный Room-кэш для офлайна.
- **Telegram** — канал быстрого ввода; общается с бэкендом по webhook.

## 5. Стек и версии (и почему)
| Что | Выбор | Почему |
|---|---|---|
| Язык сервера | Kotlin | тот же язык, что учит пользователь; не бросаем Android-навык |
| Веб-фреймворк | **Ktor 3.0.3** | родной для Kotlin, лёгкий; плагин умеет собирать фэт-джар |
| Сборка | Gradle 8.14.3 (wrapper) | wrapper скопирован из WeatherApp; отдельно ставить Gradle не нужно |
| JDK | 21 (JBR из Android Studio) | уже стоит на машине; отдельная установка не требуется |
| Kotlin | 2.1.0 | стабильная, совместима с Ktor 3.0.3 |
| БД | PostgreSQL | «взрослая» реляционная СУБД; хороший опыт для резюме |
| Доступ к БД | Exposed | Kotlin-обёртка над SQL от JetBrains |
| JSON | kotlinx.serialization | пользователь уже знает по SkyCast |
| Авторизация приложения | bearer-токен | простой и достаточный способ доказать «это я» |

Развилки, которые уже приняты: **свой бэкенд на Ktor** (а не готовый Firebase/Supabase) — чтобы учить бэкенд; **webhook** (а не long-polling) в проде — эффективнее, и у пользователя есть VPS с публичным адресом.

## 6. Инфраструктура (деплой)
На VPS пользователя **уже** работают его сайт и VPN за **nginx на порту 443**. Порт 443 занят — но отдельный порт нам и не нужен.

**Решение: добавить поддомен в существующий nginx.** nginx умеет виртуальные хосты — на один IP и порт 443 вешается много сервисов, а nginx направляет запрос по имени домена (механизм SNI / заголовок Host). Значит:
1. Заводим поддомен, например `bot.<домен>` (или бесплатный на DuckDNS), направленный на IP VPS.
2. Ktor слушает **внутренний** `127.0.0.1:8080` — наружу не торчит.
3. В nginx добавляем блок, который принимает HTTPS для поддомена и **проксирует** запросы в Ktor:
```nginx
server {
    listen 443 ssl;
    server_name bot.<домен>;

    ssl_certificate     /etc/letsencrypt/live/bot.<домен>/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bot.<домен>/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
    }
}
```
4. Сертификат для поддомена — бесплатно через **certbot** (Let's Encrypt).
5. Круглосуточную работу обеспечивает **systemd-юнит** (Linux-служба): запускает фэт-джар, перезапускает после сбоя/перезагрузки.

**И webhook бота, и API приложения** — это разные «ручки» одного Ktor за этим nginx:
- Telegram шлёт обновления на `https://bot.<домен>/telegram/<секрет>`
- приложение ходит на `https://bot.<домен>/api/...`

Приём обновлений от Telegram:
- **в проде — webhook** (Telegram сам стучится на наш адрес);
- **локально при разработке — long-polling** (бот сам опрашивает Telegram) или туннель `ngrok`/`cloudflared`, потому что у ноутбука нет публичного адреса. Код бота один и тот же — меняется только «способ доставки обновлений» (заложим переключаемым).

Запасной вариант, если поддомен не подойдёт: Telegram разрешает webhook на портах **443, 80, 88, 8443** — можно поднять отдельного слушателя на `8443`. Но поддомен через существующий nginx чище.

**Caddy НЕ используем** (хоть он и удобен автосертификатами) — он бы подрался с nginx за порт 443.

## 7. Безопасность
- **Ktor слушает только `127.0.0.1`** — из интернета недоступен, до него дотягивается лишь локальный nginx.
- **Секрет webhook.** При регистрации webhook задаём `secret_token`; Telegram присылает его в заголовке каждого запроса. Бэкенд отклоняет запрос без правильного секрета (адрес публичный — иначе кто угодно слал бы фейковые «сообщения»).
- **Авторизация приложения** — bearer-токен (строка-пропуск в заголовке запроса), чтобы данные были приватными.
- **Привязка аккаунтов** (планово). Как бэкенд поймёт, что траты из бота — «твои», и покажет их в «твоём» приложении: приложение показывает код привязки → пользователь отправляет его боту → бэкенд связывает Telegram-аккаунт с аккаунтом в приложении.
- **Секреты не коммитим.** Токен бота, пароль БД, секрет webhook — только в переменных окружения / незакоммиченных файлах (см. `.gitignore`).

## 8. План улучшений (после базового каркаса)
По важности:
1. **Ядро:** общий бэкенд+БД (есть каркас) · привязка аккаунтов · авторизация · ✅ **UUID + идемпотентность** (2026-09-02: у каждой траты уникальный id, назначает его клиент; повтор с тем же id не создаёт дубль — см. `ExpenseRepository.add`) · ✅ **общий Kotlin-модуль моделей** (2026-09-02: `shared/`, по образцу devlog — см. ниже).
2. **Сверху:** ✅ офлайн-first в приложении, полный цикл с двусторонней синхронизацией
   (2026-09-02, см. ниже) · умный парсер ввода «кофе 200» → категория+сумма (**одна логика
   на сервере**, чтобы бот и приложение считали одинаково) · реалтайм (бот добавил → телефон
   обновился: FCM-push или polling).
3. **Фичи:** бюджеты/лимиты, ✅ **теги** (2026-09-02, см. ниже), повторяющиеся траты, несколько
   валют, экспорт в CSV, графики.

### Теги (2026-09-02)

**Один тег, отдельная колонка `tag VARCHAR(50) NULL` — не отдельная таблица many-to-many.**
Рассматривались три варианта: (а) отдельная колонка на одно значение, (б) несколько тегов
через запятую в той же колонке, (в) полноценная связь many-to-many (`expense_tags` + `tags`).
Выбран (а) по трём причинам:
- **Предметная область другая, чем у тегов devlog.** У дневниковой записи в devlog теги
  извлекает ИИ из свободного текста — их естественно несколько сразу ("kotlin", "баг",
  "тестирование"). У траты пользователь вручную вписывает один короткий ярлык ("поездка",
  "подарок") — на практике это ближе к второй категории, чем к множественному набору меток.
- **Фильтрация и аналитика проще на плоской колонке.** `GET /api/expenses?tag=` — это
  `WHERE tag = ...` без JOIN; с (б) пришлось бы либо `LIKE '%tag%'` (ломается на тегах-подстроках
  друг друга, "поездка" неотличима от "поездка2"), либо парсить список на каждый запрос; с (в)
  каждый список трат тянул бы JOIN ради того, что нужно редко.
- **Миграция безопаснее.** Тот же идемпотентный `ALTER TABLE ADD COLUMN IF NOT EXISTS`, что и
  для `updated_at`/`deleted_at` — никакой новой таблицы, внешних ключей, JOIN-логики в
  `changesSince`/офлайн-кэше. Если позже понадобится несколько тегов на трату — это отдельная
  задача, которую можно начать с этой же колонки как "основного" тега.

Тег — как и остальные поля траты, участвует в идемпотентности (POST с тем же id), синхронизации
по дельте (просто едет в `Expense`/`ExpenseEntity`) и офлайн-очереди (правки с тегом уходят
через тот же `dirty`-механизм, что и правки суммы/категории) без отдельного кода — единственное
специальное место — сам фильтр `all(userId, tag)`.

Фильтр **без учёта регистра** (`Expenses.tag.lowerCase() eq tag.lowercase()`, проверено и на
Postgres, и на H2, включая кириллицу) — пользователь не обязан помнить, как именно он когда-то
напечатал тег. На веб-странице фильтр по тегу — **клиентский** (JS-функция `matchesTagFilter`,
применяется в `applyView()` наравне с фильтром по периоду), а не прямой вызов
`GET /api/expenses?tag=` для основного списка: если бы `allData` при активном фильтре содержала
только траты с этим тегом, карточка "Бюджет на месяц" (она читает `allData` без учёта тега)
считала бы "потрачено за месяц" по неполным данным — пользователь увидел бы, что бюджет почти
не тронут, хотя реальные траты просто не попали в выборку. `GET /api/expenses?tag=` при этом
не бесполезен: это сама точка входа для любого другого клиента (например, будущего экспорта
"все траты с тегом Х отдельным файлом"), и она отдельно покрыта тестами бэкенда.

### Периодическая чистка старых надгробий (2026-09-02)

`TombstoneCleaner` — демон-поток (тот же паттерн, что и `TelegramBot.startBot`: `thread(isDaemon
= true)`, без корутин — вся работа синхронный SQL через Exposed, сетевых вызовов нет). Раз в
сутки (`CHECK_INTERVAL`) физически стирает мягко удалённые траты старше 90 дней (`RETENTION`) —
обе константы задокументированы прямо в коде: 90 дней с большим запасом покрывает даже
устройство, которое не выходило на связь почти три месяца (к этому моменту у него и так истечёт
курсор синхронизации и, скорее всего, токен доверия — пользователь всё равно столкнётся с
полной пересинхронизацией).

Предикат "подлежит ли надгробие удалению" (`isDueForPurge`) вынесен в чистую функцию отдельно
от SQL-запроса (`ExpenseRepository.deleteTombstonesOlderThan`) — протестирован без базы;
сам SQL-запрос проверен интеграционным тестом на H2 и живьём на реальном Postgres.

_Известный компромисс, общий с devlog: если офлайн-клиент не синхронизировался дольше
`RETENTION`, а надгробие к этому моменту уже физически стёрто, клиент никогда не получит сигнал
об удалении через `/api/expenses/changes` и "воскресит" трату локально при следующей
синхронизации. Это внутренне присуще самой идее garbage collection для tombstone-based
синхронизации (не только в этом проекте) — при 90-дневном запасе считается приемлемым, а не
недоработкой._

### Офлайн-first и синхронизация (2026-09-02, доведено до полного цикла)

**Схема БД: `updated_at` + `deleted_at`, без Flyway.** Devlog для аналогичной задачи завёл
Flyway — там это оправдано (5 таблиц, внешние ключи, база уже реально живёт на сервере брата
с настоящими данными, миграции копятся одна за другой). У Expenses другая ситуация: одна
таблица `expenses` без FK, схема эволюционирует крайне редко (за всю историю проекта — два
предыдущих добавления столбца, `category_group` и `user_id`, оба уже сделаны безопасным
идемпотентным способом через `SchemaUtils.create` + `ALTER TABLE ... ADD COLUMN IF NOT
EXISTS`). Заводить Flyway (новая зависимость, папка миграций, тест на соответствие миграций
и `Tables.kt`, правка `CLAUDE.md`/команд сборки) ради ещё одного такого же добавления —
непропорциональная цена. Решение: остаться на существующем идемпотентном подходе, но
описать его явно как осознанный выбор (а не "забыли завести миграции"). Если схема начнёт
меняться чаще или появятся связанные таблицы с FK — Flyway тогда будет оправдан, и именно
поэтому в devlog он есть, а здесь пока нет.

Миграция в `configureDatabase()`: `updated_at` добавляется как nullable, бэкфиллится
`= created_at` для всех существующих строк (лучшее доступное приближение — момент создания
и есть последнее известное изменение для записи, которую с тех пор не трогали), и только
потом переводится в `NOT NULL` — так миграция не падает на уже существующих данных.
`deleted_at` остаётся nullable всегда (кроме удалённых строк). Проверено вживую: запущено
дважды подряд на реальном Postgres с уже существующей (без новых колонок) строкой — первый
запуск успешно смигрировал схему и бэкфиллил данные, второй прошёл как no-op без ошибок.

**Мягкое удаление и его последствия.** `DELETE /api/expenses/{id}` теперь не стирает строку
физически, а проставляет `deleted_at`+`updated_at` и чистит `note` (заметка — самое
чувствительное свободнотекстовое поле; категорию и сумму, в отличие от, например,
дневниковых записей devlog, чувствительными не сочли — оставлены как есть, это упрощает код
и не создаёт риска для приватности, сопоставимого с произвольным текстом). Каждое место,
которое трогает таблицу `expenses`, пересмотрено:
- `GET /api/expenses`, `expenses.csv`, `expensesWithoutGroup` (переклассификация ИИ) —
  фильтруют `deleted_at IS NULL`, удалённые траты в них не попадают.
- `updateExpense`, `updateGroup` — оба обновляют `updated_at`; `updateExpense` дополнительно
  отказывается редактировать уже удалённую трату (`deleted_at IS NULL` в условии).
- `add` — как и раньше, `updated_at = created_at` при создании.

**`GET /api/expenses/changes?since=&limit=`** (по аналогии с devlog) отдаёт изменения по
возрастанию `updated_at`, включая надгробия (`deleted: true`); `hasMore = expenses.size ==
limit` (та же эвристика, что у devlog: не идеальна на самой границе, но не даёт ложных
отрицательных — в худшем случае один лишний пустой запрос). `Expense` из `shared/` получил
`updatedAt: String = ""` и `deleted: Boolean = false` — оба со значением по умолчанию, чтобы
разбор не падал на гипотетическом старом ответе без этих полей (следуя правилу самого
`shared/`: у необязательных полей всегда есть дефолт).

_Отступление от devlog:_ там `updated_at`/`created_at` — `Instant` (UTC, колонка `timestamp()`),
здесь — `LocalDateTime` (без зоны, колонка `datetime()`), потому что `created_at` в Expenses
уже был `LocalDateTime` с самого начала проекта; заводить в одной таблице два разных
представления времени было бы только источником ошибок сравнения.

**Android: полный цикл офлайн-очереди.** `ExpenseEntity` (Room) получил `dirty` (правка после
подтверждения сервером — ждёт `PUT`) и `pendingDelete` (удалено локально — ждёт `DELETE`) в
дополнение к уже бывшему `synced`. `ExpensesRepository.sync()` шлёт накопленное в порядке
удаления → правки → новые траты (чтобы не тратить сеть на `PUT` для записи, которую следом
всё равно удалят), затем тянет изменения дельтой (`pullChanges`) вместо полного списка,
постранично, пока `hasMore`. Курсор синхронизации (`SyncCursorStore`, как `TokenStore`) —
для СЛЕДУЮЩЕЙ СТРАНИЦЫ внутри одного вызова используется `updatedAt` последней полученной
записи (не `serverTime` ответа: сервер формирует `serverTime` уже после запроса, и
теоретическая запись, попавшая в базу ровно в этот момент, выпала бы из выдачи навсегда с
курсором по `serverTime`); для СЛЕДУЮЩЕГО вызова `sync()` (сохраняется в SharedPreferences
после того, как весь цикл страниц закончен) — наоборот, `serverTime` последней страницы: он
корректен и для вырожденного случая пустой страницы, когда взять курсор из "последней записи"
попросту не из чего. `Mutex` вокруг `sync()` — на случай двух одновременных `refresh()`
(например, двойной тап), чтобы не гонять две синхронизации параллельно.

Конфликты разрешаются в чистой функции `serverWins(serverUpdatedAt, existing)`: если
локальной строки нет — сервер побеждает тривиально; если строка полностью синхронизирована —
тоже (перезаписывать нечем не рискуя); если есть неотправленная локальная работа (правка,
удаление или ещё не отправленное добавление) — побеждает более свежая по `updatedAt`, считая
её как `LocalDateTime`, а не строку (`LocalDateTime.toString()` не дополняет дробную часть
секунды нулями одинаковой длины — лексикографическое сравнение строк временами перепутало бы
победителя; это отдельно проверено тестом на конкретном примере `"...0.5"` против `"...0.450"`).

**Сознательно не сделано:** периодическая чистка старых надгробий (как и в devlog — "на
будущее"); переключение между разными аккаунтами на одном телефоне решено грубо — выход из
аккаунта стирает локальный кэш и курсор синхронизации целиком (`ExpensesDatabase.clearAll`,
вызывается из `MainActivity` при логауте), включая неотправленные офлайн-правки, если такие
были на момент выхода. Без этого второй аккаунт на том же телефоне увидел бы чужие
(предыдущего пользователя) траты из кэша — это перевешивает риск потерять несинхронизированную
правку при выходе без предварительной синхронизации.

Тесты: бэкенд — 34 (H2), в т.ч. новые на мягкое удаление, `updated_at`, `changesSince`
(фильтр по `since`, `limit`, изоляция между пользователями, надгробия старых по времени
создания, но новых по `updated_at` записей). Android — 18: `ExpenseDaoTest` (Robolectric,
10 тестов на очередь `synced`/`dirty`/`pendingDelete`) и `ServerWinsTest` (чистый JVM-тест
без Room/сети, 8 тестов на разрешение конфликтов). Живая проверка на реальном Postgres:
миграция (дважды, включая идемпотентность), `GET /api/expenses/changes` (до/после добавления
и удаления, тело надгробия). **На реальном телефоне не проверялось** (среда без экрана).

### Общий модуль моделей (`shared/`)
Контракт API (DTO трат, бюджета, регистрации) раньше был описан дважды — на бэкенде и в
Android-приложении; несовпадение полей превращалось из ошибки компиляции в молчаливый баг
в рантайме. Теперь это отдельный Gradle-проект `shared/` (Kotlin/JVM, JVM-байткод 17 — чтобы
его читал и Android, и бэкенд на JDK 21), подключается к обеим сборкам через `includeBuild`,
без версии в Maven-смысле — просто как соседний модуль. Пакет тот же, что у бэкенда
(`io.github.vihrea1337.expenses`), поэтому серверному коду не нужно ничего импортировать;
Android-приложение импортирует явно. Контрактные тесты (имена полей в JSON, обратная
совместимость со старым клиентом) — в `shared/src/test`.

Веб-страница (`backend/src/main/resources/static/index.html`) в этот модуль **не переведена**
осознанно: она чистый JS без сборки, TypeScript/Kotlin-модуль ей просто негде было бы
скомпилировать без появления в проекте фронтенд-тулчейна — непропорциональная цена для
нескольких полей.

---

## Глоссарий
- **API** — «дверца» для запросов к серверу; набор адресов-ручек, на которые можно послать запрос и получить данные.
- **REST API / эндпоинт** — стиль API поверх HTTP; эндпоинт — конкретный адрес-ручка (например, `/api/expenses`).
- **Бэкенд** — серверная часть: программа, работающая круглосуточно на удалённом компьютере и отвечающая на запросы.
- **Ktor** — Kotlin-фреймворк для написания серверов (и клиентов).
- **Netty** — «мотор» HTTP-сервера внутри Ktor, принимает сетевые соединения.
- **Фэт-джар (fat jar)** — один `.jar`-файл, внутри которого и твой код, и все библиотеки; удобно запускать на сервере одной командой.
- **PostgreSQL** — реляционная база данных (таблицы, SQL).
- **Exposed** — Kotlin-библиотека для работы с БД: запросы пишутся на Kotlin.
- **kotlinx.serialization** — превращает Kotlin-классы в JSON и обратно.
- **webhook** — Telegram сам присылает POST-запрос на наш публичный адрес при каждом сообщении.
- **long-polling** — наоборот: наш бот сам периодически спрашивает Telegram, нет ли новых сообщений.
- **reverse proxy** — «привратник» (nginx) перед приложением: принимает внешние запросы, расшифровывает HTTPS и передаёт их внутрь.
- **TLS/SSL, сертификат** — шифрование канала (`https`); сертификат — «паспорт» домена, подтверждающий подлинность.
- **certbot / Let's Encrypt** — бесплатная автоматическая выдача и продление сертификатов.
- **systemd** — менеджер служб в Linux; следит, чтобы программа работала постоянно и перезапускалась.
- **bearer-токен** — секретная строка-пропуск в заголовке запроса, доказывающая, кто его шлёт.
- **идемпотентность** — повторная отправка того же запроса не создаёт дубликат.
- **FCM** — сервис Google для доставки push-уведомлений на телефон.
- **SNI** — механизм, по которому сервер понимает, к какому домену обращается клиент (позволяет одному порту 443 обслуживать много сайтов).
