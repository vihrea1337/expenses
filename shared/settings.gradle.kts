// Общий модуль Expenses: один набор моделей обмена (DTO) для бэкенда и Android-приложения.
// Собственный Gradle-проект, который подключается к обеим сборкам через includeBuild.
rootProject.name = "shared"
